#import <UIKit/UIKit.h>

/// Root view controller — manages the main UI and navigation.
/// In real projects, this is where you set up tab bars, navigation, etc.
@interface RootViewController : UIViewController
@end
