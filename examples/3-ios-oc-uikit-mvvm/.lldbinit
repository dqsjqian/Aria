# ios-oc-mvvm 项目级 lldb 启动脚本
# ----------------------------------------------------------------
# 作用范围：仅当通过 Xcode Scheme 显式引用此文件时加载（见下方说明）。
# 与本项目无关的其他 Xcode 工程 / 命令行 lldb 不受影响。
#
# 规则说明：
#   1) 不让 lldb 拦截 SIGTERM
#      Xcode 顶部 Stop（⌘.）/ 关闭模拟器时会向 app 发 SIGTERM，
#      默认 lldb 会停在 main，弹出红色 "Thread 1: signal SIGTERM"。
#      设 -s false 后 lldb 直接放行，进程干净退出，不再打断调试。
#
#   2) 顺手放过 SIGPIPE（写入已关闭 socket 时常见，绝大多数库已自行处理）。
# ----------------------------------------------------------------

process handle SIGTERM -n false -p true -s false
process handle SIGPIPE -n false -p true -s false
