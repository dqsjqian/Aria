// Root build.gradle.kts — 5-android-jni-compose-mvvm
//
// Standalone Gradle project (not in main CMake build).
// Consistent with iOS demos which are standalone Xcode projects.

plugins {
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false
}
